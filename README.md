# WinOS Vercel

Standalone WinOS web desktop code extracted from the larger project.

## Run locally

```bash
npx vercel dev
```

This repository intentionally contains only the WinOS/Vercel frontend and API support files.
